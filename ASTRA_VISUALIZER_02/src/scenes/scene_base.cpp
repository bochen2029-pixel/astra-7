// scene_base.cpp - intentionally empty in V1. The interface in scene_base.h
// is pure-virtual + has default no-op implementations; scenes derive directly
// without needing shared code yet. V4+ will pull common helpers in here.
#include "scenes/scene_base.h"
